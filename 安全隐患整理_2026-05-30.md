# 安全隐患整理

> 生成时间：2026-05-30
> 范围：混合检索问题已解决后的剩余安全风险

## 说明

当前默认同时检索企业知识库和个人知识库的设计可以保留。安全边界应定义为：默认可以查个人库，但只能查“当前已认证用户自己的个人库”，不能查全部个人库，也不能信任请求体里自报的用户身份。

## 高风险问题

### 1. 个人库 owner 不匹配时会降级查全部个人库

位置：

- `app/retrieval/retriever.py`
- `_search_with_owner_fallback`

当前逻辑在 `personal_kb_strict_owner_filter=False` 时，如果按 `owner_open_id` 查不到结果，会改用：

```python
metadata_filter={"knowledge_base_type": "personal"}
```

这等价于查询所有个人知识库。单人测试阶段能解决身份不一致导致的查不到问题，但多用户正式环境会造成跨用户个人资料泄露。

建议：

- 生产环境必须关闭这个 fallback。
- `personal_kb_strict_owner_filter` 生产默认应为 `True`。
- 如果 owner 不匹配，应返回空个人库结果，而不是扩大到全部个人库。

### 2. `/ask` 接口没有可信认证主体

位置：

- `app/main.py`
- `/ask`

当前 `/ask` 直接接收 `AskRequest`，其中 `channel`、`user_id` 都来自请求体。正式环境中，调用方可以伪造：

```json
{
  "channel": "feishu",
  "user_id": "任意用户ID"
}
```

如果后续权限判断依赖这些字段，就会产生越权风险。

建议：

- `/ask` 必须加 API Key、JWT、Session 或反向代理认证。
- 检索用的 owner 必须来自认证上下文，而不是请求体。
- 请求体中的 `user_id` 只能作为业务参数或展示字段，不能作为权限主体。

### 3. `personal_kb_strict_owner_filter` 默认值偏危险

位置：

- `app/config.py`

当前默认：

```python
personal_kb_strict_owner_filter: bool = False
```

如果生产环境忘记配置环境变量，会默认启用宽松个人库检索。

建议：

- 默认值改为 `True`。
- 如需单人测试宽松检索，应通过明确的本地开发配置开启。
- 启动时如检测到生产环境且该值为 `False`，应记录高危日志或拒绝启动。

## 中风险问题

### 4. 飞书卡片确认在缺失 open_id 时不会拒绝

位置：

- `app/main.py`
- `process_feishu_card_action`

当前逻辑：

```python
if action_data.get("open_id") and action_data.get("open_id") != pending.get("open_id"):
    ...
```

如果 `action_data.get("open_id")` 为空，条件不会触发，流程会继续执行确认保存。

建议：

- `open_id` 缺失时直接拒绝。
- 只有 `open_id` 存在且等于 pending 中的上传者 open_id 时，才允许确认保存。

### 5. CORS 允许所有来源

位置：

- `app/main.py`
- `CORSMiddleware`

当前配置：

```python
allow_origins=["*"]
allow_methods=["*"]
allow_headers=["*"]
```

在接口未认证或认证较弱时，开放 CORS 会扩大浏览器侧滥用面。

建议：

- 生产环境限制为可信前端域名。
- 管理类接口、上传接口、知识库接口不应对任意来源开放。

### 6. 个人文件权限依赖临时内存态

位置：

- `app/main.py`
- `pending_feishu_files`

个人文件保存确认依赖进程内字典 `pending_feishu_files`。这在单进程测试中可用，但在多进程、多实例、重启场景下会丢失状态，也不便于统一审计。

建议：

- 正式环境改为 Redis 或数据库存储。
- pending 记录中保存上传者身份、chat_id、message_id、过期时间。
- 确认动作必须校验操作者身份和 pending 状态。

## 生产前最低安全要求

1. 保留默认企业 + 个人混搜，但个人库只能查当前认证用户自己的数据。
2. 禁止 owner 不匹配时查全部个人库。
3. `/ask` 使用认证上下文确定用户身份。
4. `personal_kb_strict_owner_filter=True` 作为生产默认。
5. 飞书卡片操作缺失 `open_id` 时拒绝。
6. 生产环境限制 CORS 来源。

